début exercice 6.4

début variables locale

var text: T <- ""
var sousCahine: T <- ""
var début: N <- 0
var fin: N <- 0
var scan: N <- 0

fin variables locales

si fin > longueur(text) + 2 OU fin < début OU début = fin

	alors,
	afficher "les position saisies ne permettent pas d'effectuer une sous-chaîne de caractères"

	sinon,
	afficher "Veuillez saisir un texte"
	saisir text
	afficher "veuillez saisir la position du premier caractère de la sous chaine de texte"
	saisir début
	afficher "veuillez saisir la position du dernier caractère de la sous chaine de texte"
	saisir fin
	scan <- début

	tant que scan < fin

		sousChaine <- sousChaine (+) caract(text, scan)
		scan <- scan + 1

	fin tant que

	afficher sousChaine

fin si

fin exercice 6.4