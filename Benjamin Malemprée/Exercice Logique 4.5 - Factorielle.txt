début exercice 4.5

début variable locale
var entre: N <- 0
var i: N <- 0
var réponse: N <- 1
fin variables locales

afficher "Veuillez saisir un chiffre (strictement positif ou égal à 0) dont on calculera la factorielle."
saisir entre

tant que entre < 0

	afficher "une factorielle n'est pas possible sur un chiffre négatif. Veuillez saisir un chiffre strictement positif ou égal à 0."
	saisir entre

fin tant que

	si entre = 0
		alors, afficher "La factorielle de 0 = 1"
		sinon,
			tant que i =/= entre

				réponse <- reponse * (i + 1)
				i <- i + 1

			fin tant que
		
			afficher "la factorielle de ", entre, " = ", reponse
	fin si

fin exercice 4.5